// Google Sign-In OAuth callback URL scheme (must equal REVERSED_CLIENT_ID from
// GoogleService-Info.plist). After `flutterfire configure` or downloading a new
// plist from Firebase, copy the REVERSED_CLIENT_ID value here so it matches.
GOOGLE_REVERSED_CLIENT_ID=com.googleusercontent.apps.510334238921-REPLACE_WITH_IOS_CLIENT_ID
